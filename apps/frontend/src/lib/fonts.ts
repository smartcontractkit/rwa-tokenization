import { Fira_Code } from 'next/font/google' // Figtree

// export const figtree = Figtree({
//   subsets: ['latin'],
//   variable: '--font-sans',
// })

export const firaCode = Fira_Code({
  subsets: ['latin'],
  variable: '--mono-space',
})
